CREATE DATABASE cadastro;


--------------

USE cadastro; -- Certifique-se de estar usando o banco de dados "cadastro"

CREATE TABLE usuario (
    usuario_id INT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(100),
    email VARCHAR(100) UNIQUE,
    senha VARCHAR(255),
    perfil VARCHAR(50)
);
CREATE TABLE cliente (
    cliente_id INT PRIMARY KEY AUTO_INCREMENT,
    usuario_id INT,
    nome VARCHAR(100),
    email VARCHAR(100),
    endereco VARCHAR(255),
    telefone VARCHAR(20),
    data_nasc DATE,
    cpf VARCHAR(14),
    numero_cartao VARCHAR(16),
    FOREIGN KEY (usuario_id) REFERENCES usuario(usuario_id)
);
CREATE TABLE funcionario (
    funcionario_id INT PRIMARY KEY AUTO_INCREMENT,
    usuario_id INT,
    nome VARCHAR(100),
    cpf VARCHAR(14),
    email VARCHAR(100),
    cargo VARCHAR(100),
    salario DECIMAL(10,2),
    matricula VARCHAR(20),
    data_contratacao DATE,
    endereco VARCHAR(255),
    FOREIGN KEY (usuario_id) REFERENCES usuario(usuario_id)
);



--------
USE cadastro; -- Certifique-se de estar usando o banco de dados "cadastro"

CREATE TABLE produtos (
    produto_id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL,
    descricao TEXT,
    preco DECIMAL(10, 2) NOT NULL,
    estoque INT NOT NULL,
    imagem VARCHAR(255),
    tipo VARCHAR(50)
);

tabelas novas


CREATE TABLE pedido (
    id INT PRIMARY KEY AUTO_INCREMENT,
    cliente_id INT,
    produto_id INT,
    quantidade INT,
    data_pedido DATE,
    
    endereco_entrega VARCHAR(255),
    valor_total DECIMAL(10,2) -- Adicionando a coluna para o valor total da compra
);
CREATE TABLE pagamento (
    id INT PRIMARY KEY AUTO_INCREMENT,
    pedido_id INT,
    valor DECIMAL(10,2),
    data_pagamento DATE,
    metodo_pagamento VARCHAR(50),
    numero_do_cartao VARCHAR(16),
    
    FOREIGN KEY (pedido_id) REFERENCES pedido(id)
);


link funcionario

http://localhost/sparta/adm/usuario_adm/login_adm.php




